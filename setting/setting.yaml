defaults:
  - _self_
  - model: dev
  - data: dev

tasks: [ "preprocess" ]

log:
  dir: resource/log/

model_checkpoint:
  dir: resource/model_checkpoint/

ranking:
  dir: resource/ranking/

result:
  dir: resource/result/


eval:
  metrics: [ "ndcg", "precision" ]
  thresholds: [ 1,5,10 ]
  num_nearest_neighbors: 100
  label_cls: [ "tail", "head" ]
